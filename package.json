{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:dependencies": "lerna bootstrap",
    "test:front": "lerna run test --scope=front-end",
    "test": "lerna run test",
    "commit": "git add . && git-cz"
  },
  "devDependencies": {
    "git-cz": "^4.9.0",
    "lerna": "^6.6.2"
  }
}
