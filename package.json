{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:dependencies": "lerna bootstrap",
    "test:front": "lerna run test --scope=front-end",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
