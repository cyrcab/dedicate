import { View, Text } from 'react-native';

export default function Auctions({ route }) {
//   const { event } = route.params;
//   console.log(event);
  return (
    <View>
      <Text>salut</Text>
    </View>
  );
}
